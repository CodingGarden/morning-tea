/**
 * 
 * @param {*} a 
 * @param {*} b 
 */
function add(a, b) {
  return a + b;
}

/**
 * This function takes in 2 numbers and subtracts them
 *
 * @param {Number} a
 * @param {Number} b
 * @returns {Number}
 */
function subtract(a, b) {
  return a - b;
}

const result = subtract(['🔴'], ['🌻']);
console.log(result);

console.log(5 + '5');

const wat = Array(16).join('wat' - 1) + ' Batman!';
console.log(wat);


